Options -Indexes
<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /kemahasiswaan/
    
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^(.*)$ index.php?/$1 [L]

    # Remove HTTPS redirection for local WAMP
</IfModule>

Options -Indexes